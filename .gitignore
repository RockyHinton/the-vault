node_modules

dist

.DS_Store

server/public

vite.config.ts.*

*.tar.gz

.env
.env.*
!.env.example

# Playwright output (holds the disposable e2e database name while a run is active)
test-results
playwright-report

# Local file storage for development (bytes live in object storage in production)
.vault-data
