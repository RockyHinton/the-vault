We are making targeted refinements to the Projects page (design only).

Do NOT change layout, functionality, or card sizing.

--------------------------------------------------
1. FIX STAGE LABEL TYPOGRAPHY
--------------------------------------------------

Update stage labels on project cards:

- Change from ALL CAPS to Title Case:
  - Evaluation
  - Development
  - Production
  - Archived

- Styling:
  - keep font size slightly smaller than title
  - use medium weight (not bold)
  - apply slight letter-spacing (very subtle)
  - reduce opacity slightly (not pure white)

Goal:
Stage labels should feel calm, premium, and understated — not loud UI elements.

--------------------------------------------------
2. FIX PROJECT CARD IMAGES NOT RENDERING
--------------------------------------------------

Ensure every project card displays a background image.

If images were previously added but are not showing:
- properly bind image URLs to the card background

Implementation:

For now (design phase):
- assign hardcoded cinematic placeholder images to each project card

Use cinematic images as if they were the cover or a still from a film.

Technical requirements:
- images must fully cover the card (background-size: cover)
- maintain aspect ratio
- ensure overlay gradient still allows text readability

Fallback logic:
If a project has no image:
→ use a default stage-based image

--------------------------------------------------
3. DO NOT CHANGE
--------------------------------------------------

- Do NOT change card size
- Do NOT change grid layout
- Do NOT change three-dot menu functionality
- Do NOT change navigation or filters logic

--------------------------------------------------
SUCCESS CRITERIA
--------------------------------------------------

- Stage labels feel more refined and less “dashboard-like”
- All project cards clearly display images
- Cards feel cinematic and alive instead of empty or flat

At the end, confirm:
- how images were applied (hardcoded or fallback)
- that stage labels were updated from ALL CAPS to Title Case