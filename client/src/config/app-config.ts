// Deployment-specific branding. Each production company receives a dedicated
// deployment; this is the one place its display name lives. Security and
// session policy are owned by the server, not by client config.
export const APP_CONFIG = {
  clientName: "The Vault",
};
